import { Model.FixedAccount } from './model.fixed-account';

describe('Model.FixedAccount', () => {
  it('should create an instance', () => {
    expect(new Model.FixedAccount()).toBeTruthy();
  });
});
